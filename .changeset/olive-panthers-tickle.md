---
"spotlight-testing": patch
---

Update the toolchain to TypeScript 7, Node 24, and the latest ultracite/oxlint/oxfmt/tsdown, and pin the build to unprefixed `.js` output so the package `bin`, `main`, `types`, and `exports` entry points keep resolving.
